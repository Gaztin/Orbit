import org.apache.tools.ant.taskdefs.condition.Os

buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
    // Fix gradle file paths exceeding the file path character limit on Windows
    if (Os.isFamily(Os.FAMILY_WINDOWS) && buildDir.length() > 32) {
        buildDir "C:/Temp/"
    }
}
